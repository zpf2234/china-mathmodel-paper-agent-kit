---
name: cumcm-model-tournament
description: CUMCM 关键问题的候选模型竞技与创新性审计技能。用于以预先冻结的同口径标准比较透明基线、主候选、异构挑战和必要的精确小实例或上下界，淘汰不适配、不可识别或无法验证的路线，防止按算法高级感选模型。
---

# CUMCM 模型竞技场

## 何时使用

每个会实质改变最终答案的关键问题必须调用。基本机理唯一时，把不同离散、解析特例或独立实现作为挑战者；不得为满足数量伪造方法对比。

## 竞技协议

试算前写 `求解/模型竞技场.json`，冻结：

- 统一题意口径、训练/验证数据和约束；
- 透明基线、主候选、异构挑战、fallback 的角色；
- 硬淘汰条件和评分指标；
- 数值容差、随机种子、时间预算；
- 何时只能报告近优、区间或并列候选。

候选依次通过题意、单位/量纲、定义域、约束、可识别性、数据泄漏和可复现性硬门槛。存活候选再比较独立验证、稳健性、答案质量、复杂度、计算代价和可解释性。优化问题增加可行性、界/gap、小实例精确解或扩大邻域证据；逆问题增加合成回收、目标剖面、多初值和参数相关性；预测问题使用严格留出或时间外推。

## 创新审计

写 `求解/创新贡献表.md`，只保留能改变结论、精度、验证或解释的贡献。换算法名称、堆模型、调配色、增加无效指标不算创新。每条贡献绑定公式、结果差异、验证和适用边界。

## 输出与门禁

在 `求解/候选淘汰记录.md` 逐项记录执行状态、淘汰证据和保留原因。主模型必须经至少一个不同主要误差机制的挑战；候选差异不超过数值/抽样不确定性时不得制造唯一胜者。

完成条件：比较协议先于结果冻结；所有声称执行的候选有运行产物；主模型选择可由证据复核；失败路线仍保留足够的挑战证据。否则 `BLOCK_MODEL_SELECTION`。
