---
name: cumcm-model-writing
description: 把 CUMCM 已验证的求解证据写入第五部分“模型的建立与求解”。用于按问题或共享模型组织变量、机理、目标、约束、公式推导和具体求解过程，交代参数来源、数值步骤、停止条件与精度，并为后续结果验证留出衔接；结构由题目关系和本地 50 篇优秀论文普查共同校准，不暴露代码、文件过程或开放性评价。
---

# CUMCM 模型的建立与求解

先完整读取 [../cumcm-paper/references/section-chain-contract.md](../cumcm-paper/references/section-chain-contract.md)、
[../cumcm/references/model-selection-and-answer-quality.md](../cumcm/references/model-selection-and-answer-quality.md) 与
[../cumcm-paper/references/model-result-content-standard.md](../cumcm-paper/references/model-result-content-standard.md)。
使用 [../cumcm-paper/assets/latex-template/model-establishment-solution.tex](../cumcm-paper/assets/latex-template/model-establishment-solution.tex)
作为第五部分模板。只消费 `cumcm-solve` 已冻结的模型、参数和真实求解记录。

## 50 篇语料给出的结构依据

本地普查显示：23/50 篇采用分问题一级章，21/50 篇采用总章统领后按问题二级展开；模型建立与
求解分别在 46/50、49/50 篇中可明确定位，29/50 篇对每一问都显式设置模型与求解标题。这些结果
说明模型和求解必须可定位，但不要求每问机械复制同一套标题。

本项目按用户指定固定使用：

```text
五、模型的建立与求解
```

第五部分内部再依据问题关系组织：

- 各问相对独立：按“问题一的模型建立与求解、问题二的模型建立与求解……”设置二级标题。
- 多问共享坐标系、状态方程或基础关系：先设一个信息明确的共享模型二级标题，再逐问展开新增条件与求解。
- 单问内容较短：模型建立与求解连续成文，不强制拆成两个三级标题。
- 单问存在两块以上充实内容：使用带对象或动作的三级标题，如“运动关系与状态方程”“参数辨识与数值积分”，不只写“模型建立”“模型求解”。

## 国一模式：严密且直观

用户目标为国一、国奖或一等奖时，完整读取
[../cumcm/references/national-first-precision-and-visual-gates.md](../cumcm/references/national-first-precision-and-visual-gates.md)。
机理/物理/数学题必须先过数值精度门禁，再把关键几何、状态、临界构型和计算链转成正文视觉证据。
复杂求解过程必须引用对应的分问题流程图；图中节点和阈值要与本节公式、算法及实际脚本一致。
每问至少形成“解释图 + 结果/验证图”的双证据；只给公式和表格不视为直观完成。

## 优秀论文规范表达范本与防 AI 味指南（基于 50 篇语料）

50 篇优秀论文在“模型的建立与求解”中展现出极高的数学严谨性与推导流畅度。严禁出现“为了解决问题，我们建立了如下公式”或大段代码解释。

### ❌ AI 味典型表达 vs ✅ 50 篇优秀论文规范表述

| 维度 | ❌ AI 味套路表达 | ✅ 优秀论文规范数理推演（50 篇对标） |
|---|---|---|
| **公式引出** | “为了求解浮子的位移，我们根据相关的物理学定律，建立了如下公式：” | “对浮子与振子分别进行受力分析。设 $x_1(t), x_2(t)$ 分别为浮子与振子的垂荡位移，根据牛顿第二定律，系统的运动方程组为：” |
| **公式解释** | “其中 $m_1$ 代表浮子的质量，$c$ 代表阻尼系数，这两个参数在模型中起到了非常关键的作用。” | “式中：$m_1$ 为浮子质量；$f(t) = F_0 \cos(\omega t)$ 为波浪激励力；$F_d = c(\dot{x}_1 - \dot{x}_2)$ 为 PTO 系统的直线阻尼力。” |
| **推导过程** | “经过复杂的数学推导与代入化简，我们可以轻松得到最终的微分方程表达式。” | “将状态变量定义为 $\mathbf{Y}(t) = [x_1, \dot{x}_1, x_2, \dot{x}_2]^T$，原二阶方程组转化为等价的一阶状态空间方程：” |
| **参数来源** | “根据题目给出的丰富数据以及经验估计，我们合理地设定了模型的各项参数。” | “模型参数取值如下：浮子质量 $m_1 = 4000\,\text{kg}$（题面给定），阻尼系数 $c$ 在 $1000\sim 100000\,\text{N·s/m}$ 范围内扫描，海浪激励频率 $\omega = 1.40\,\text{rad/s}$。” |
| **算法描述** | “我们编写了 MATLAB 代码，利用遗传算法和 ODE45 函数运行求解，得到了极好的拟合效果。” | “采用四阶 Runge-Kutta 法在 $t \in [0, 40\,\text{s}]$ 内进行数值积分，设置固定步长 $\Delta t = 0.01\,\text{s}$。算法收敛标准为相邻周期做功相对误差小于 $10^{-4}$。” |

### 规范写作纪律与表达克制

1. **精选最正确的模型与假设**：在求解竞技后，正文建模部分**直奔主题呈现最终精选的最正确模型**。对试算中废弃的其他候选模型或失真假设，不作大篇幅推导。
2. **选择思考极度克制，大部分用图概括**：对模型选择的思考、对比与权衡，正文中仅用**一小段（1-2句）精准交代淘汰理由**；复杂的候选比较、框架推导与算法迭代链，**优先使用高质量结构图、算法流程图或模型对比图**展现。
3. **公式前冒号引出，公式末尾不加标点**（保持标准 LaTeX 编写习惯）。
4. **推导用因果链衔接**：使用“将式(3)代入式(4)得…”、“对上式求关于 $c$ 的一阶导数，令其等于 0：”、“两端取 Fourier 变换可得…”，严禁使用“通过简单的计算”、“显而易见”等轻浮词汇。
5. **彻底清除软件与代码痕迹**：严禁出现 `df.fit()`、`x0=[0,0]`、`gurobi_solver()`、`csv文件`、`第15行代码` 等编程信息。正文只讲数学模型与算法机理。

## 详细模型建立

按题型写清读者复核所需内容：

1. 界定研究对象、变量、状态、目标和约束；
2. 从物理机理、统计结构、几何关系或决策逻辑推出模型；
3. 给出必要的定义域、边界条件、初始条件和参数来源；
4. 展开本题特有的关键推导，省略教材式通用算法介绍；
5. 说明模型假设在何处进入公式或约束；
6. 只有真实候选会改变答案时，才写有证据的模型取舍。

每组核心控制方程必须承担六项责任：①物理定律或数学关系来源；②变量定义域与单位；③初始/边界条件；④近似及适用条件；⑤方程如何产生题目要求的输出；⑥离散或估计误差如何受控。任一责任缺失时不得放行。数值求解还必须区分模型误差、参数误差和数值误差，并说明步长、网格、容差或停止条件对最终有效位的影响；求解器返回 `success` 或单条收敛曲线不能代替精度预算。

优化题明确决策变量、目标函数和全部约束；统计题明确样本、响应、误差结构、估计量与识别条件；
机理题明确守恒、受力、几何和边界；逆问题明确前向模型、观测关系、可识别性和正则约束；
决策与仿真题明确状态、动作、转移、收益或随机机制。

## 具体求解过程

把问题分析中的高层路线在本章落实为可复核的数学与数值步骤。按真实执行顺序说明：

- 输入量如何进入模型，必要的变换、离散或初始化如何完成；
- 参数、状态或决策变量按什么关系求取；
- 算法每一步更新什么量，相邻步骤传递什么对象；
- 搜索范围、约束处理、步长、容差、随机重复或多初值策略；
- 判停条件、收敛标准和最终报告精度；
- 前问输出如何作为后问输入或共享参数。

标准算法只说明本题实际使用的部分和题目特定改造，不抄教科书流程。正文说明数学过程，不展示
代码、运行命令、软件界面、文件路径或调试记录。

## 公式与行文

同时遵守 [../cumcm/references/mathematical-writing.md](../cumcm/references/mathematical-writing.md)
和 [../cumcm/references/argumentation-patterns.md](../cumcm/references/argumentation-patterns.md)。

- 变量先说含义再给符号，最后定义域和单位；不在公式后才解释。
- 每组公式前用一句话说明"为了什么目的，需要建立什么关系"。
- 本题特有的关键推导必须展开；标准代入和化简省略但说明依据。不逐行抄教科书也不跳跃过大。
- 边界条件和初始条件与主方程紧邻放置，不藏在段落中间。
- 独立展示公式全部编号，引出句以中文冒号结束，公式末尾不加标点。
- 同一语义组的并列坐标、状态方程或约束可使用左大括号与 `aligned`，普通推导不强行套用。
- 参数注明题面来源、估计方式、标定依据或取值范围；来源不明的参数值不得出现。
- 多参数模型使用参数表（符号—含义—数值—单位—来源），按
  [../cumcm/references/table-design.md](../cumcm/references/table-design.md) 设计。
- 每个结论性断言在 ±2 段内有数值、图表或公式支持。
- 文字只说明对象、关系和过程，不写"方法先进、效果显著、便于理解"等评价。

## 与其他章节的边界

- 问题分析只保留高层思路和路线，具体推导与求解步骤全部在第五部分展开。
- `cumcm-model-writing` 完成模型与求解正文后，由 `cumcm-results-validation` 在相应问题位置补入结果、验证和直接结论；无需另起一个空泛总章。
- 模型优缺点、改进和推广不写入第五部分。

## 严格禁写

- 只有算法名称而没有目标、约束、参数、更新和停止条件；
- 未运行候选、内部失败日志和无法回到求解证据的步骤；
- 代码、脚本、CSV/JSON、文件名、工作表名、运行命令和附录入口；
- “为了满足可计算性”“本文的回答”“计算口径”等元叙事；
- 未经证据支持的“全局最优、唯一解、高精度、鲁棒性强”；
- 为凑篇幅重复算法原理、题面或问题分析内容；
- 为每个公式单设标题或机械复制三级标题。

## 门禁

写入 `审查/section-chain/gates/model-writing.json`，逐问检查：

- `fifth_section_structure`：模型建立与求解位于第五部分，分问或共享模型结构清楚；
- `variables_objective_constraints_complete`：对象、变量、目标和约束齐全；
- `assumptions_used`：相关假设在模型中实际调用；
- `derivation_continuous`：关键关系和推导连续；
- `parameters_traceable`：参数来源或估计方式明确；
- `solution_process_reproducible`：具体求解步骤、停止条件和精度可复核；
- `question_handoff_clear`：跨问输入输出衔接准确；
- `no_open_evaluation_or_irrelevant_text`：没有开放性评价和无关内容；
- `no_production_trace`：没有代码、文件和调试过程痕迹。

任何模型无法产生题目所需输出时退回 `cumcm-solve`，不能靠文字补齐。通过后交给 `cumcm-results-validation`。
